let sum = (x,y) => {
 return x+y;
}

let diff = (x,y) => {
    return x-y;
}

let multiply = (x,y) => {
    return x*y;
}

let divison = (x,y) => {
    return x/y
}

module.exports = {
    sum,
    diff,
    multiply,
    divison,
};